# Either set a key here, or run `ant auth login` and leave this unset.
ANTHROPIC_API_KEY=

# Optional. Defaults to claude-opus-5.
# ASHBY_MODEL=claude-sonnet-5

# Optional. Defaults to config/resume.pdf.
# ASHBY_RESUME=config/resume.pdf

# Pipeline 2 (embeddings). Anthropic has no embeddings endpoint.
OPENAI_API_KEY=
